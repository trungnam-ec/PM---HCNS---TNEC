# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# Script nháp / chạy một lần. Trước đây 19 script kiểu này nằm ở thư mục gốc
# dashboard/ và 90 file trong scratch/ — trong đó có cả script XOÁ DỮ LIỆU
# hàng loạt (delete-all-reports.js, cleanup-*.js) đọc thẳng .env.local và chạy
# được ngay bằng `node`. Để lẫn trong repo là rủi ro chạy nhầm mất dữ liệu thật.
# Cần thử nghiệm gì thì để trong scratch/ — nó không vào git nữa.
/scratch/

# Tài liệu nội bộ — CỐ Ý không đưa lên repo.
# Hai file trong này mô tả kiến trúc phân quyền và chính sách bảo mật của công ty
# (sơ đồ luồng duyệt, danh sách cờ quyền, cách siết RLS). Giữ ở máy để tra cứu,
# không phát tán theo mã nguồn. Đã từng bị `git add .` gom nhầm lên một lần.
/docs/

# Bản gốc file mẫu Excel còn nguyên số liệu thanh toán thật của công ty. Giữ
# lại trên máy để đối chiếu, KHÔNG đẩy lên: mọi thứ nằm dưới public/ đều tải
# được qua HTTP mà không cần đăng nhập.
/_local/

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

# cấu hình chạy preview tại máy, không đẩy lên
.claude/
